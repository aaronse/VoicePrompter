node_modules/
dist/
.DS_Store
*.local
.env
.env.*
!.env.example

.obsidian/

# Upstream ignores AGENTS.md (an empty Claude Cowork artifact, 62c64a3). This fork
# tracks it deliberately: it is the agent contract, and CLAUDE.md imports it.
